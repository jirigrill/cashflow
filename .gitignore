# data — never commit real financial data
data/
*.csv

# Worktrees symlink `data` to the real folder; `data/` above does not match a
# symlink, so name it without the slash too.
data

# Screenshots render the real figures — same data, different format.
prototype/shots/

# secrets
.env
.env.*
credentials*.json
service-account*.json

node_modules/
dist/
.DS_Store
