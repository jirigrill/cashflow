{
  "name": "cashflow-prototype",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "THROWAWAY prototype for wayfinder ticket #8 — exists to be argued with, not extended.",
  "scripts": {
    "dev": "vite",
    "verify": "node --experimental-strip-types verify.ts",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "echarts": "^5.5.1",
    "papaparse": "^5.4.1"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.14",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
